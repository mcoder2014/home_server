# 文件分享验证记录

基线：master `1db41295444ded2f9b7a19a0e34c7b40e05fdc8f`。日期：2026-09-19。验证对象：文件分享、密码阅读、兼容行为与部署入口。

## 修改前基线

| 检查 | 环境 | 实际结果 |
|---|---|---|
| Go全仓构建 | Pi隔离源码、Go1.21.13，`go build -p 2 ./...` | 通过 |
| 既有主CI包 | 独立MariaDB10.11和Redis，合成数据 | 325个顶层用例通过，20个既有opt-in用例跳过；首次3个过期用例因测试库时区失败，对齐CI的UTC后3/3重跑通过 |
| DAL | 主测试后串行执行 | 13/13通过 |
| 前端行为 | macOS Node24，现有依赖 | 92/92通过 |
| 浏览器 | 本机Chrome、仅loopback合成fixture | 说明书8/8、账号6/6通过 |
| Python与技能 | 本机合成HTTPS fixture | 33/33通过 |
| 方案HTML | Chrome桌面1440px和手机390px | 无横向溢出；1个评论根，10个唯一语义ID；Mermaid已实际渲染 |
| 文档权限 | 托管API与匿名HTTP | owner、enabled、版本读回一致；匿名404 |

上述基线只用于识别回归，不计作新文件分享或密码门禁已通过。隔离数据库的NOW()原为东八区，与测试DSN的UTC解释不一致；只调整测试实例时区，未修改生产配置或业务代码。

## 新功能验收

| 检查 | 实际结果 |
|---|---|
| Pi全仓构建 | Go1.21.13，`go build -p 2 ./...` 通过 |
| 主CI回归 | 337个其他包顶层用例通过；3个新增文件HTTP用例定向修复后全部通过，共340个；20个既有opt-in用例跳过 |
| 数据层 | 原14个DAL用例通过；新增敏感SQL日志回归后FileShare两个用例通过，合计15个 |
| 文件HTTP | 多分享、匿名/登录/成员/密码、单次并发上限、撤销/到期/删除、HEAD/Range/缺文件不耗次、空文件、慢上传撤销会话、跨实例容量竞争通过 |
| 密码阅读 | 网页HTML/子资源/HEAD/Range/raw/评论、说明书详情/文件/缩略图/列表摘要、旧授权随版本失效通过 |
| 前端 | `npm run test:frontend` 111/111；`npm run build` 通过；本次修改文件ESLint通过 |
| 浏览器 | Chromium文件7、说明书11、全站视觉2、账号中心6，共26/26；桌面1440与手机320截图已实际复核 |
| Python | API客户端13、网页技能15、说明书技能5、部署示例校验2，共35/35通过 |
| 配置 | 公开部署示例检查通过；两入口网关候选配置在真实Nginx上`-t`通过；隔离故障测试确认普通上游错误有日志、敏感令牌路径无日志，尚未生效 |

全量lint仍存在原有4项问题，位于Common.vue（2项）、AddBook.vue（1项）、ScanCodePage.vue（1项）；本次修改行未引入新的lint问题。Go各包共享测试库时串行执行，所有数据均为隔离合成数据。首次新增fixture配置与锁态JSON断言错误已修正后重跑，未放宽生产权限。

已合入master `f125d3f` 的说明书安全PDF预览优化；合并后前端110个测试、说明书11个浏览器用例与生产构建再次通过。API入口的分享页面跳转完整前端入口，避免缺少SPA静态资源或登录路由。

独立审查与最终发布使用PR固定提交；生产备份、切换和健康检查结果由服务器发布manifest记录，不将生产配置或凭据写入仓库。

## SQL执行计划

在隔离MariaDB10.11中各建立2000条合成文件和分享记录、20个所有者。本人游标列表命中 `idx_files_owner_id`（range，估算90行）；token查找命中 `uk_file_shares_token`（const，1行）；单文件分享列表命中 `idx_file_shares_file_id`（ref，1行）；用户容量聚合命中 `idx_files_owner_id`（ref，估算100行）。该结果证明所核对查询没有全表扫描；容量聚合仍按用户文件数线性读取，受每用户文件数上限约束。

## 独立审查修复验证

gpt-5.6-sol max 对PR进行了独立静态审查。已修复敏感URI错误日志、失效浏览器Cookie阻断公开分享、所有者评论历史兼容性、登录回跳query泄露令牌四类问题。Pi文件HTTP三项和网页密码评论定向回归通过；前端111个测试与文件浏览器7个用例通过，实际登录文档请求只有`/login`且登录后返回原分享。Nginx故障测试确认敏感URI不记错误日志。密码表已有记录或查询失败时，部署脚本拒绝自动退回不识别密码的旧后端。
